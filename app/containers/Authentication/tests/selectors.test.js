// import { fromJS } from 'immutable';
// import { selectAuthenticationContainerDomain } from '../selectors';

describe('selectAuthenticationContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
