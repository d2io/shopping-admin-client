/*
 * AuthenticationContainer Messages
 *
 * This contains all the text for the AuthenticationContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AuthenticationContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthenticationContainer container!',
  },
});
